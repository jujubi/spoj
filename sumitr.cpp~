#include<iostream>
#include<vector>
using namespace std;
vector<int> arr;
vector<int> inp;
int main()
{
	int test;
	scanf("%d",&test);
	while(test--)
	{
		int n;
		scanf("%d",&n);
		arr.clear();
		vector<int> temp;
		temp.resize(n+1,-1);
		arr.resize(n+1,temp);
		
		


	}
	return 0;
}
